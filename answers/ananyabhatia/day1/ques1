#include <stdio.h>
#include <math.h>

int main()
{
     int n , a, i;
    printf("enter number n");
    scanf ("%d", &n);
   
     for (i=1;i<= n;i++)
     {
         a = pow (i,3) + i*2;
         printf ("%d\n" , a);
     }
}
